extends layout.pug 
block contents 

    div(class="form")

        if(error)
            b #{error.msg}
        
        if(errMess)
            b= errMess 

        form( method="POST" , action="")
            div(class="form-label")
                label(for="admin") Admin code


            div(class="form-field")    
                input(type="password",placeholder="admin secret key", name="admincode")

            
            button(type="submit") submit         